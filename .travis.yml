language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-fx-prototype' ]; then cd .. && eval "mv $currentfolder generator-fx-prototype" && cd generator-fx-prototype; fi

